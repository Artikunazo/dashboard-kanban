import { SubtaskDoneDirective } from './subtask-done.directive';

describe('SubtaskDoneDirective', () => {
  it('should create an instance', () => {
    const directive = new SubtaskDoneDirective();
    expect(directive).toBeTruthy();
  });
});
