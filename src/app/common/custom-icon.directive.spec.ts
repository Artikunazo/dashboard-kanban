import { CustomIconDirective } from './custom-icon.directive';

describe('CustomIconDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomIconDirective();
    expect(directive).toBeTruthy();
  });
});
